name: party_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+5

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # 결제 상태·금액 정본·판매 통계 집계의 정본 — 호스트 앱과 관리자 웹이 함께 쓴다.
  partychu_sales:
    path: ../packages/partychu_sales
  # 달력·시계 등 Material 기본 위젯의 한국어 표기(요일·'날짜 선택'·'확인').
  flutter_localizations:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  cloud_functions: ^5.1.3
  # 승인제 파티의 신청자 제출 사진 **전용**. 파티/플레이스의 일반 이미지는
  # 예전 그대로 Cloudflare R2(공개 URL)를 쓴다 — 신청 사진만 여기로 온 이유는
  # "호스트와 관리자만 볼 수 있어야 한다"를 공개 URL로는 만족시킬 수 없기
  # 때문이다. 접근 판정은 storage.rules가 한다.
  firebase_storage: ^12.3.2
  # 푸시 알림 — 토큰 발급/갱신과 메시지 수신(firebase_messaging), 앱이 열려
  # 있을 때의 알림 표시(flutter_local_notifications), 권한 4상태 구분과
  # 시스템 설정 열기(permission_handler).
  #
  # firebase_messaging만으로도 권한을 요청할 수는 있지만 "거부"와 "다시 묻지
  # 않음"을 가려내지 못한다 — 그 둘을 구분해야 설정앱으로 보낼지, 다시 물을지
  # 정할 수 있어서 permission_handler를 함께 쓴다.
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.3.1
  cupertino_icons: ^1.0.8
  hugeicons: ^1.1.7
  google_sign_in: ^6.2.1
  kakao_flutter_sdk_user: ^1.8.0
  kakao_flutter_sdk_share: ^1.8.0
  share_plus: ^13.2.0
  flutter_naver_login: ^2.1.1
  flutter_naver_map: ^1.4.4
  image_picker: ^1.2.2
  image_cropper: ^9.1.0
  video_player: ^2.11.1
  # 웹 HLS 재생 — Cloudflare Stream이 주는 .m3u8은 Chrome/Edge/Firefox의
  # <video>가 네이티브로 읽지 못한다(Safari만 읽는다). video_player_web이
  # 만든 그 엘리먼트에 hls.js를 붙이려고 플러그인 클래스를 직접 확장하므로
  # 전이 의존성이 아니라 직접 의존성으로 적는다(그 패키지 README의 안내).
  # 구현은 lib/utils/web_video_hls_web.dart 하나뿐이고, 웹이 아닌 빌드에서는
  # 조건부 import가 그 파일을 아예 컴파일하지 않는다.
  video_player_web: ^2.4.0
  video_player_platform_interface: ^6.6.0
  web: ^1.1.0
  video_compress: ^3.1.2
  geolocator: ^13.0.2
  shared_preferences: ^2.3.0
  http: ^1.2.2
  table_calendar: ^3.1.2
  intl: ^0.20.0
  flutter_dotenv: ^5.2.1
  flutter_native_splash: ^2.4.6
  webview_flutter: ^4.10.0
  visibility_detector: ^0.4.0+2
  device_info_plus: ^13.2.0
  package_info_plus: ^10.2.0
  url_launcher: ^6.3.2
  app_links: ^6.3.3
  video_trimmer: ^5.0.0
  # QR 이용권 — 발급(렌더링)은 qr_flutter, 사장님 스캔은 mobile_scanner.
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.4.1
  flutter_web_plugins:
    sdk: flutter
  flutter_volume_controller: ^2.0.1
  # 짧은 UI 효과음(찜 성공) 재생 전용 — 음원은 tool/gen_favorite_pop_sound.dart가
  # 만든 assets/sounds/favorite_pop.wav 하나뿐이다. iOS는 ambient 카테고리로
  # 붙여 무음 스위치를 그대로 따르고, 안드로이드는 오디오 포커스를 잡지 않아
  # 재생 중인 음악을 끊거나 줄이지 않는다(lib/services/ui_sound_service.dart).
  audioplayers: ^6.1.0
  app_settings: 7.0.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true

  assets:
    - .env
    - assets/icons/
    - assets/icon/
    - assets/images/
    # UI 효과음 — 코드로 합성한 것만 들어간다(tool/gen_favorite_pop_sound.dart).
    - assets/sounds/
    # 지하철역 좌표(공공데이터를 변환해 넣는 JSON) — 형식·출처는
    # assets/data/README.md 참고. 비어 있어도 앱은 정상 동작한다(지하철 행만 숨김).
    - assets/data/subway_stations.json

  fonts:
    - family: SeoulHangang
      fonts:
        - asset: assets/fonts/SeoulHangangL.ttf
          weight: 300
        - asset: assets/fonts/SeoulHangangM.ttf
          weight: 500

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: "assets/icon/app_icon_large.png"
  image_path: "assets/icon/app_icon_large.png"

flutter_native_splash:
  color: "#FFC5D8"
  image: assets/images/splash.png
  android_12:
    color: "#FFC5D8"
    image: assets/images/splash.png
