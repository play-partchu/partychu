name: partychu_sales
description: 파티츄 결제 상태·금액 정본·판매 통계 집계 — 호스트 앱과 관리자 웹이 함께 쓰는 정본.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
